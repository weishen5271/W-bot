# HEARTBEAT

## 每周检查
- 检查 `configs/app.json` 与示例配置是否一致演进。
- 检查 `README.md` 是否覆盖最近新增能力。
- 检查 `skills/` 是否存在失效或重复技能。

## 每日检查
- 记录当天关键变更到 `memory/YYYY-MM-DD.md`。
- 若产生稳定偏好/事实，写入 `MEMORY.md`。
- 复盘未完成事项，确保有下一步动作。

## 触发条件
- 当出现连续失败或回归时，立即执行一次专项排查心跳。
